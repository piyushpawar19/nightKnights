dense_weight: 0.5
bm25_weight: 0.5
feature_weight: 0.5
rerank_top_k: 20
feature_thresholds:
  skill_match: 0.7
  experience_fit: 0.6
normalization_method: "min_max"
# Configuration for the Candidate Ranking stage

scoring_weights:
  skill_match: 0.35
  experience_relevance: 0.30
  education_alignment: 0.15
  cultural_signals: 0.10
  llm_refinement: 0.10

honeypot_detection:
  disqualification_threshold: 0.10 # Max rate of honeypots in top 100 before DQ
  suspicious_patterns:
    - "keyword_stuffing"
    - "behavioral_twins"
    - "tier5_lies"
    - "impossible_profiles"

ranking_loop:
  max_iterations: 3
  beam_search_size: 150
