import os
import sys
import pytest

# 'src' prefix is now used properly across test files

# You can specify which tests to run, or let pytest discover them
# For example, to run all tests in the 'tests' directory:
pytest.main(["tests/"])
